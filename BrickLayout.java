public class BrickLayout{
    private int numRows;
    private int numCols;
    private Brick[][] layout;

    public BrickLayout(int numRows, int numCols) {
        this.numRows = numRows;
        this.numCols = numCols;
        layout = new Brick[numRows][numCols];
        //finish later
    }
}